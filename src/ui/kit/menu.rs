// use super::input::InputEl;

// pub trait MenuChildren {
//     fn child(&self, idx: usize) -> &impl InputEl;
//     fn child_mut(&mut self, idx: usize) -> &mut impl InputEl;
//     fn size(&self) -> usize;
// }

// /// Menu is a layout container for inputs and buttons
// pub struct Menu {
//     // children:  
// }
